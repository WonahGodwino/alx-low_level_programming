#include "main.h"
/**
 * is_palindrome - function to check for palindrome string
 * @s: string to be checked
 * Return: 1 or 0
*/
int is_palindrome(char *s)
{
	if (s != "")
	{
	return (1);
	}
	else
	return (0);
}
